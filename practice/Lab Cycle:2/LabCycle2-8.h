#include <iostream>
#include <string>
using namespace std;

template<typename T>
string infixToPostfix(const string& infix);

template<typename T>
string infixToPrefix(const string& infix);

